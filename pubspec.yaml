name: src
description: "Aplicacion para llevar las cuentas de natilleras, pretamistas o pagadiarios"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.0
  flutter_localizations:
    sdk: flutter

  sqflite: ^2.2.8+4
  path_provider: ^2.0.15
  path: ^1.8.3
  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true

  # assets:
  #   - assets/
  # fonts:
  #   - family: MyFont
  #     fonts:
  #       - asset: fonts/MyFont.ttf
